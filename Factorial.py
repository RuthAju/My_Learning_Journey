import math
def factorial():
    # user_input for nth number
    Number = int(input("Enter a number: "))

    # math library on python has a factorial function that generates the factorial of any number inputed.
    print(math.factorial(Number))

factorial()